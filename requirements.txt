# Reported (CPU) environment for the paper's experiments.
# Matches the manuscript: PyTorch 2.12, sentence-transformers 5.5, CPU-only.
# Note: on Windows, PyPI torch is CPU-only by default; on Linux it may bundle
# CUDA, but every reported number is measured on CPU (see README).
torch==2.12.0
sentence-transformers==5.5.1
transformers==5.11.0
scikit-learn==1.9.0
pandas==3.0.3
numpy==2.4.6
psutil==7.2.2
openpyxl==3.1.5
